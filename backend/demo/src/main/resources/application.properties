# PostgreSQL Configuration
# Local PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/advocare
# spring.datasource.username=postgres
# spring.datasource.password=Pass@123

# PostgreSQL on Render (Cloud)
spring.datasource.url=jdbc:postgresql://dpg-d4tiou7pm1nc7398orig-a.singapore-postgres.render.com:5432/sachin_borse
spring.datasource.username=sachin_borse_user
spring.datasource.password=QhKqckD25CVJjMSAi0W2tIRww7F5KNBk

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.leak-detection-threshold=60000

# Required because Render requires SSL
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# File Upload Configuration (for Cloudinary uploads)
# Increase max file size to 10MB (Cloudinary allows up to 10MB)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Geocoding API Keys (Optional - Nominatim works without keys)
# Get free API keys from:
# LocationIQ: https://locationiq.com/free-tier (60 requests/day)
# OpenCage: https://opencagedata.com/api (2,500 requests/day)
# geocoding.locationiq.key=your_locationiq_api_key_here
# geocoding.opencage.key=your_opencage_api_key_here

# Cloudinary Configuration
# Get your credentials from: https://cloudinary.com/console
cloudinary.cloud-name=dea8ytcdu
cloudinary.api-key=171486794796692
cloudinary.api-secret=eIIa6C5UeoaAyYObmclZNLoVumY

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGeneration12345678901234567890
jwt.expiration=86400000
